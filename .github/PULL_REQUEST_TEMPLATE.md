<!-- Thank you for contributing to lexsync. -->

**What does this change do?**
A short description, and a link to the issue it addresses if there is one.

**Checklist**

<!-- R variant -->
- [ ] `devtools::document()` has been run if the roxygen comments changed.
- [ ] `devtools::test()` passes, and new behaviour has tests.
- [ ] `devtools::check()` is clean.
- [ ] Documentation and `NEWS.md` are updated where relevant.

<!-- Python variant
- [ ] `pytest` passes, and new behaviour has tests.
- [ ] `ruff check` is clean.
- [ ] The docs build (`mkdocs build --strict`) and `CHANGELOG.md` is updated where relevant.
-->

- [ ] No secret (API key, token) appears anywhere in the diff.
