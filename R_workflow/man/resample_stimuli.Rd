% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matching.R
\name{resample_stimuli}
\alias{resample_stimuli}
\title{Produce several disjoint matched item sets (items as a random factor)}
\usage{
resample_stimuli(pool, design, schema, n_sets, verbose = FALSE)
}
\arguments{
\item{pool}{A candidate pool with the \code{match_on} dimensions present.}

\item{design}{A parsed design configuration.}

\item{schema}{The parsed global schema.}

\item{n_sets}{Number of disjoint matched sets to draw.}

\item{verbose}{Logical; passed to \code{\link[=match_stimuli]{match_stimuli()}}.}
}
\value{
A data frame of matched stimuli with an added \code{replicate} column. The
replicates are bound together, which drops the \code{"audit"} attribute
\code{\link[=match_stimuli]{match_stimuli()}} uses to report a relaxed tolerance window, so a relaxation
inside a replicate reaches the console under \code{verbose} but not the run log or
the datasheet.
}
\description{
Each replicate is an independent, fully matched set drawn from the pool with the
items of earlier replicates removed, so no item is reused. This lets a study
treat its items as a random factor (running different item samples across
participant groups, or showing an effect holds across samples) instead of treating
them as a fixed set (Clark, 1973; Yarkoni, 2022). Deterministic and identical to the
Python engine.
}
