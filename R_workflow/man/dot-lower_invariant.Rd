% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/querying.R
\name{.lower_invariant}
\alias{.lower_invariant}
\title{Lower-case a character vector under the Unicode default case mapping}
\usage{
.lower_invariant(x)
}
\arguments{
\item{x}{A character vector, or a vector coercible to one.}
}
\value{
A character vector, lower-cased; \code{NA} is preserved.
}
\description{
Base R's \code{tolower()} hands case mapping to the C library, so it is both
locale- and platform-dependent (see \code{?chartr}): under a C or 8-bit locale it
leaves accented capitals uncased, and even under a UTF-8 locale it applies
only the simple mappings, rendering Greek final sigma as U+03C3 and dropping
the dot of U+0130. Python's \code{str.lower()} always applies the Unicode default
\emph{full} mapping, giving U+03C2 and i + U+0307 respectively. \code{word} is the
canonical key behind every byte-order tie-break, so the engines must fold
case identically; pinning ICU to the root locale ("und") reproduces Python's
mapping exactly and removes the ambient locale from the result.
}
\keyword{internal}
