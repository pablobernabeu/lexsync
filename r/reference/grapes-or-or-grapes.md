# Null-coalescing operator

Returns `a` unless it is `NULL`, in which case it returns `b`.

## Usage

``` r
a %||% b
```

## Arguments

- a, b:

  Values; `a` is returned unless `NULL`.

## Value

`a` or `b`.
