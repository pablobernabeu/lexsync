# Attested subsyllabic constituents keyed by "role\|length" with integer counts

Attested subsyllabic constituents keyed by "role\|length" with integer
counts

## Usage

``` r
build_constituent_inventory(reference_words)
```
