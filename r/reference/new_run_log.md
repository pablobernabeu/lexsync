# Start a new run log

Start a new run log

## Usage

``` r
new_run_log(name, meta = list())
```

## Arguments

- name:

  A label for the run.

- meta:

  A named list of run-level metadata (seed, versions, ...).

## Value

A run-log object (a list).
