# Normalise line endings: store and check out text files as LF.
* text=auto eol=lf

# Binary files (never normalise or diff as text).
*.png binary
*.docx binary
*.pdf binary
*.RData binary
*.Rdata binary
